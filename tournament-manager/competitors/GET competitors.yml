info:
  name: GET competitors
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/{{api}}/competitors"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
