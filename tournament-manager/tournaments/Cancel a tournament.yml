info:
  name: Cancel a tournament
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/{{api}}/tournaments/cmmw3c5hj0000p86xlbuzdv9c/cancel"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
